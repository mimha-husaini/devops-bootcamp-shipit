{
  "name": "shipit-board",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Mission Control — the shared ws hub + Three.js spectator for Ship It",
  "scripts": {
    "test": "node --test"
  }
}
